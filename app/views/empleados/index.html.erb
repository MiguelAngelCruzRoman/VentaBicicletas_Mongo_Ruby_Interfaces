<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CRUD de Empleados</title>
  <style>
    table {
      border-collapse: collapse;
      width: 100%;
    }
    th, td {
      border: 1px solid #dddddd;
      text-align: left;
      padding: 8px;
    }
    th {
      background-color: #f2f2f2;
    }
  </style>
</head>
<body>
  <h1>CRUD de Empleados</h1>
  <a href="/empleados/new"><button>Agregar Empleado</button></a>
  <table>
    <thead>
      <tr>
        <th>Nombre Completo</th>
        <th>Correo</th>
        <th>Teléfono</th>
        <th>Puesto</th>
        <th>Fecha de Contratación</th>
        <th>Acciones</th>
      </tr>
    </thead>
    <tbody>
      <% @empleados.each do |empleado| %>
        <tr>
          <td><%= empleado.primerNombre %> <%= empleado.segundoNombre %> <%= empleado.apellidoPaterno %> <%= empleado.apellidoMaterno %></td>
          <td><%= empleado.correo %></td>
          <td><%= empleado.telefono %></td>
          <td><%= empleado.puesto %></td>
          <td><%= empleado.fechaContratacion %></td>
          <td>
            <a href="<%= edit_empleado_path(empleado) %>"><button>Editar</button></a>
  
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</body>
</html>
