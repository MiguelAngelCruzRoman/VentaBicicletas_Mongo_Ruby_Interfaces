<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CRUD de Tiendas</title>
  <style>
    table {
      border-collapse: collapse;
      width: 100%;
    }
    th, td {
      border: 1px solid #dddddd;
      text-align: left;
      padding: 8px;
    }
    th {
      background-color: #f2f2f2;
    }
  </style>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</head>
<body>
  <h1>CRUD de Tiendas</h1>
    <%= link_to "Agregar", new_tienda_path, class: "btn btn-success" %>
  <table>
    <thead>
      <tr>
        <th>Nombre</th>
        <th>Teléfono</th>
        <th>Horario</th>
        <th>Dirección</th>
        <th>Acciones</th>
      </tr>
    </thead>
    <tbody>
      <% @tiendas.each do |tienda| %>
        <tr>
          <td><%= tienda.nombre %></td>
          <td><%= tienda.telefono %></td>
          <td><%= tienda.horario %></td>
          <td>
              <% if tienda.direccion %>
              <%= tienda.direccion.calle %>, <%= tienda.direccion.ciudad %>, <%= tienda.direccion.estado %>, <%= tienda.direccion.pais %>
            <% else %>
              Dirección no especificada
            <% end %>
          </td>
          <td>
            <%= link_to edit_tienda_path(tienda), class: "btn btn-warning" do %>
              Editar
            <% end %>            
            <%= button_to "Eliminar", tienda_path(tienda), method: :delete, data: { confirm: '¿Estás seguro que deseas eliminar esta tienda?' }, class: 'btn btn-danger' %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</body>
</html>
